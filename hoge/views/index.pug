doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    title=title
  body
    h1=title
    p Welcome to #{title}
    section
      each msg in messages
        div
          h2 #{msg.username}
          time #{msg.date}
          p #{msg.message}